#include "stdafx.h"
#include "Graph.h"

graph::graph(int size):size(size){
	//adj_list.resize(size);
}

void graph::addEdge(int node1, int node2){
	//adj_list[node1 - 1].push_back(node2 - 2);
	//adj_list[node2 - 1].push_back(node1 - 1);
}

std::vector<int> graph::connected_component(){
	std::vector<int> r;
	return r;
}